<!--
  Never actually rendered — `+page.ts`'s `load` unconditionally redirects
  to `/stacks` before this component would mount. Exists only because
  SvelteKit requires a `+page.svelte` for `/` to be a valid route at all.
-->
